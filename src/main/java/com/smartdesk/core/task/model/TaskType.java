package com.smartdesk.core.task.model;

/**
 * Defines the high-level categories a task can belong to. The categories align
 * with the requirements of the Task module blueprint and allow for targeted
 * filtering in the dashboard.
 */
public enum TaskType {
    TODO,
    COURSE,
    ANNIVERSARY,
    EVENT
}
